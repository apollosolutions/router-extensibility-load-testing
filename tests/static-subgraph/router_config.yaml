# yaml-language-server: $schema=../../router.config.json
supergraph:
  listen: 0.0.0.0:4040
  path: /
  introspection: true
headers:
  all:
    request:
      - insert:
          name: "source"
          value: "config"
